# ProBG Blog 1.1.1 — Test plan

- Home layout + Menu output: home page must complete normally and render the blog menu once.
- Home layout + Latest Articles output: home page must complete normally and render the article block once.
- Direct `/blog` page: full blog listing must render normally.
- Blog category page: category listing must render normally.
- Blog article page: article must render normally with full breadcrumb hierarchy.
- ProBG Blog assigned to a layout also used by the blog section: nested module call must render only the configured block and must not recursively render the full blog page.
- After package update run Extensions → Modifications → Refresh before testing.
